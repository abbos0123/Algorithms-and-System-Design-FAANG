--------------------------Complexity---------------------------
Time complexity: O(n*loh(n))
Space complexity: O(1)


-----------------------------Code------------------------------
class Solution {
   public int addDigits(int num) {
        if (num < 10)
            return num;
        else {

            while (num >= 10) {
                int c = num;
                int p = 0;
                while (c > 0) {
                    p += c % 10;
                    c = c / 10;
                }
                num = p;
            }
            return num;
        }
    }
}
